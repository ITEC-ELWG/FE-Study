【Blog(PHP)】

 *  blog_user为本项目的数据库文件， 初始用户yx密码是1111

 *  数据库连接失败时没有采用try catch捕获异常， 而是采用@符号屏蔽了warning，
    然后将错误提示打印出来

 *  数据加密只在php端利用password_hash()进行了加密

 *  数据过滤这一部分内容还是没有弄的太懂， 感觉若是想要完全过滤的话需要用到很
    多过滤函数进行处理， 此次任务中只利用filter_var()函数对中文字符进行了过滤